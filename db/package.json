{
  "name": "snakeio-db",
  "version": "1.0.0",
  "description": "Database schema and migrations for Snake.io",
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "prisma:studio": "prisma studio",
    "prisma:seed": "ts-node prisma/seed.ts"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0"
  },
  "devDependencies": {
    "@types/node": "^20.3.1",
    "prisma": "^5.14.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  }
}
